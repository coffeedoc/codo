guard :coffeescript, :input => 'src', :output => 'lib'

#guard :shell do
#  watch(%r{^(spec/.+_spec\.coffee)$}) { |m| `mocha -u bdd #{ m[1] }` }
#  watch(%r{^src/(.+)\.coffee$})       { |m| `mocha -u bdd spec/#{ m[1] }_spec.coffee` }
#end
